import 'package:flutter/material.dart';

class ViolinTab extends StatelessWidget {
  const ViolinTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("ViolinTab"),
    ));
  }
}
