import 'package:flutter/material.dart';

class GuitarTab extends StatelessWidget {
  const GuitarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("GuitarTab"),
    ));
  }
}
